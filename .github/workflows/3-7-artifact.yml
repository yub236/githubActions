name: 3-7-artifact

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Create file
        run: echo "Hello Artifact" > result.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: result
          path: result.txt